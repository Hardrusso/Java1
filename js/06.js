// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.1); // Siempre Redondea hacia arriba, muestra 3
resultado = Math.floor(2.9);// Floor redondea hacia abajo, muestra 2
resultado = Math.sqrt(144); // sqrt Raiz cuadrada
resultado = Math.abs(-2.9); // abs convierte numero en positivo
resultado = Math.min(3, 5, 1, 8,11,12); // min muestra el numero menor
resultado = Math.max(3, 5, 1, 8,11,2154); // max muestra el numero mayor
resultado = Math.random(); // numero aleatorio
resultado = Math.floor(Math.random()*151); // funcion: coge un numero random, multiplicalo por 151 y el resultado redondealo hacia abajo


console.log(resultado);